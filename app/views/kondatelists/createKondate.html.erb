<h2><%= @msg %></h2>
<h2>献立表作成</h2>
<p>作成する献立表の期間を入力してください</p>
<%= form_tag(controller: "kondatelists", action: "createKondate") do %>
<tr>
    <th>開始日</th><td><%= date_field_tag "startDate" %></td>
    &emsp;～&emsp;
    <th>終了日</th><td><%= date_field_tag "endDate" %></td>
</tr>
<%= submit_tag("献立表作成") %>
<% end %>
<table border="1">
    <% if @date.present? %>
        <% @date.each do |date| %>
            <tr><td><%= date.kondate_date %></td></tr>
        <% end %>
        <p><%= button_to "献立表削除" data: {confirm: "献立表を削除します。よろしいですか？"} %></p>
    <% end %>
</table>